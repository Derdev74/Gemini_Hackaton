# ═══════════════════════════════════════════════════════════════
# Just Travel App — Environment Variables Template
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your API keys

# ── CRITICAL (Required for AI agents to work) ──────────────────
# Google Gemini API Key — Powers all 6 AI agents
# Get it at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_gemini_api_key_here


# ── OPTIONAL (Graceful mock fallback if missing) ───────────────

# OpenWeatherMap API Key — 5-day weather forecast
# Get it at: https://openweathermap.org/api
# Without it: Uses deterministic hash-based mock weather data
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here

# Google Maps/Places API Key — Place search, geocoding
# Get it at: https://console.cloud.google.com/apis/credentials
# Without it: Returns sample mock places
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Neo4j Graph Database — Destination discovery via CypherTools
# Get it at: https://neo4j.com/cloud/aura/
# Without it: Returns sample destinations from mock data
NEO4J_URI=neo4j+s://your-database-id.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your_neo4j_password_here

# Apify API Token — Social media trend analysis
# Get it at: https://console.apify.com/account/integrations
# Without it: Returns sample trending hashtags
APIFY_API_TOKEN=your_apify_token_here

# Amadeus Flight API — Real-time flight search
# Get it at: https://developers.amadeus.com/
# Without it: Returns sample flight data
AMADEUS_CLIENT_ID=your_amadeus_client_id_here
AMADEUS_CLIENT_SECRET=your_amadeus_secret_here

# RapidAPI Key (Booking.com) — Hotel search
# Get it at: https://rapidapi.com/DataCrawler/api/booking-com15
# Without it: Returns sample hotel listings
RAPIDAPI_KEY=your_rapidapi_key_here


# ── AUTHENTICATION ──────────────────────────────────────────────

# NextAuth Secret — Signs JWT tokens (access + refresh)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (Sign In with Google)
# Get it at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_oauth_client_id_here
GOOGLE_CLIENT_SECRET=your_google_oauth_secret_here


# ── APPLICATION SETTINGS ────────────────────────────────────────
DEBUG=false
LOG_LEVEL=INFO
